# cinnamon-rolls
roll roll roll roll roll !!!

## GitHub Pages

Run `python generate_pages.py` to build static pages for every cinnamon roll listing into the `docs/` directory. Publish that directory with GitHub Pages to give each地點 its own mini site plus an index page.
